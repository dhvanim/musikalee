import * as React from 'react';

export default function RightSideBar() {
    
    const [trending, setTrending] = React.useState([]);
    const [reccomended, setReccomended] = React.useState([]);
    
    
    return (
        <div>
            Hi
        </div>
    );
}